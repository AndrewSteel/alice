# AES-256-CBC passphrase for encrypting/decrypting IMAP passwords.
# alice-mail-reader derives the key as SHA-256(MAIL_ENC_KEY).
# Generate a strong value: openssl rand -base64 32
MAIL_ENC_KEY=change-me-to-a-strong-random-passphrase
